// const api = require('./api');

// const getAccFolders = async () => {
//   let resolvedPeer = await api.call('messages.getDialogFilters', {});
//   return resolvedPeer;
// }

// mudule.exports = getAccFolders;

// (async () => {
//   let resolvedPeer = await api.call('messages.getDialogFilters', {});
//   const ololo = [];
//   resolvedPeer[0].include_peers.forEach(element => {
//     if(element._ == 'inputPeerChannel'){ololo.push(element)};
//   });
//   console.log(ololo);
//   return resolvedPeer;
// })();

